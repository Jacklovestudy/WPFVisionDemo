# 视觉学习进度

更新日期：2026-09-09。

## 学习背景与目标

已具备较熟练的 C# 开发能力，视觉概念与基础处理从零开始。此前工作中调用过海康方案 API；用户随后明确学习目标是掌握视觉知识点、算法原理以及实现和验证方法。

本项目承接 Codex 任务“视觉”（01a08404-bec7-7c23-8a08-6bd5cf27d60a）的学习上下文，采用当前已经建立的 WPF + Prism 技术栈。原对话早期提出的 WinForms、围绕海康方案学习等建议，已被后续澄清取代。尚未确定或引入视觉库，第一课用 C# 数组即可完成。

## 学习方法

按每周 8～10 小时的 24 周计划推进，时间可以随掌握程度顺延。

1. 先解释概念和输入输出。
2. 手算小矩阵，预测结果。
3. 用 C# 实现核心过程。
4. 改参数、换输入，观察失败条件。
5. 需要时与成熟图像库比较。
6. 保存代码、实验图片和学习笔记。

## 当前状态

| 项目 | 状态 |
|---|---|
| 视觉知识体系和 24 周计划 | 已整理 |
| 第一课：图像、像素与坐标 | 练习已检查，灰度图已显示 |
| WPF + Prism 应用基础 | 已接入；用户已确认运行没问题 |
| 灰度显示、亮度调整、反转、二值化 | 已练习，整理为独立方法 |
| 四邻域与八邻域概念 | 已完成示例问答 |
| 四邻域连通域统计 | 已实现并检查，正在学习队列搜索流程 |

## 已完成的第一课练习

使用 [第一课](lesson-01-pixels.md) 中的 4 行、5 列灰度数组：

- [x] 读取并输出指定像素的灰度值。
- [x] 把右上角像素改为 `0`。
- [x] 统计灰度值为 `30` 的像素数量。
- [x] 完成二维数组到一维像素数组的转换和显示。

用户当前采用 `x` 表示行、`y` 表示列：访问为 `gray[x, y]`，展开为 `pixels[x * width + y]`。原课程的坐标命名与此不同。

用户已确认理解外层扫描、内层队列搜索逻辑。本阶段增加 ConnectedRegion 保存像素坐标、通过 Pixels.Count 获取面积，并实现按面积筛选。下一步结合 [代码阅读指南](code-guide.md) 查看坐标如何用于生成筛选后的图像。

## 与当前项目的衔接

课程材料在 `docs/vision` 中维护。后续实验以当前 WPF 应用为载体：界面负责展示和输入，ViewModel 负责交互与状态，算法根据实际实验需要逐步提取为独立 C# 代码。

后续进度以本文件为准。按面积去除四邻域小块已实现并通过算法检查；八邻域算法尚未实现。
